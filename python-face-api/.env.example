# MongoDB connection URI (shared with Node backend)
# Get this from either environment variables or your MongoDB Atlas cluster
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Frontend CORS origin
# Update this to your actual Vercel deployment URL
CORS_ORIGIN=https://aura-eight-beta.vercel.app
